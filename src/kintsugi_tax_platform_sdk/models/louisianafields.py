"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from kintsugi_tax_platform_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class LouisianaFieldsRegistrationType(str, Enum):
    r"""Registration type for this Louisiana import: the account type registered with the Louisiana Department of Revenue (Sales Tax or Remote Seller)."""

    SALES_TAX = "SALES_TAX"
    REMOTE_SELLER = "REMOTE_SELLER"


class LicenseType(str, Enum):
    r"""Louisiana license type, which determines the filing portal: Direct Marketers file using the Louisiana DOR LaTAP website, Remote Sellers file using the Remote Sellers website, and Standard license types file using the Parish E-File website."""

    DIRECT_MARKETER = "DIRECT_MARKETER"
    REMOTE_SELLER = "REMOTE_SELLER"
    STANDARD = "STANDARD"


class LouisianaFieldsTypedDict(TypedDict):
    r"""State-specific fields for Louisiana registration import.

    Louisiana splits filing across three portals depending on the license type:
    Direct Marketers file in LaTAP, Remote Sellers file in the Remote Sellers portal,
    and Standard licenses file in Parish E-File.
    """

    registration_type: LouisianaFieldsRegistrationType
    r"""Registration type for this Louisiana import: the account type registered with the Louisiana Department of Revenue (Sales Tax or Remote Seller)."""
    business_name: str
    r"""State-registered business name as shown on the Louisiana tax account."""
    la_state_tax_id: str
    r"""Louisiana state certificate number received from the state when registering for a state sales tax license (9 or 10 digits)."""
    license_type: LicenseType
    r"""Louisiana license type, which determines the filing portal: Direct Marketers file using the Louisiana DOR LaTAP website, Remote Sellers file using the Remote Sellers website, and Standard license types file using the Parish E-File website."""
    naics_code: str
    r"""6-digit NAICS code used to categorize business operations. Many codes were updated or created in 2022."""
    registered_email_address: str
    r"""Email address registered with the Louisiana Department of Revenue."""
    mfa_completed: NotRequired[bool]
    r"""Whether the customer has completed MFA setup using the Kintsugi virtual mail group ID on their Louisiana tax account."""
    last_payment_to_state: NotRequired[Nullable[str]]
    r"""Amount of the most recent payment made to Louisiana, as a plain decimal number. Required for Direct Marketer license types; enter 0 if no return has been filed yet."""
    zip_code: NotRequired[Nullable[str]]
    r"""ZIP or Canadian postal code on the Direct Marketer license registration. Required for Direct Marketer license types. Accepts a 5-digit US ZIP or a Canadian postal code (e.g. L6H0C3)."""


class LouisianaFields(BaseModel):
    r"""State-specific fields for Louisiana registration import.

    Louisiana splits filing across three portals depending on the license type:
    Direct Marketers file in LaTAP, Remote Sellers file in the Remote Sellers portal,
    and Standard licenses file in Parish E-File.
    """

    registration_type: LouisianaFieldsRegistrationType
    r"""Registration type for this Louisiana import: the account type registered with the Louisiana Department of Revenue (Sales Tax or Remote Seller)."""

    business_name: str
    r"""State-registered business name as shown on the Louisiana tax account."""

    la_state_tax_id: str
    r"""Louisiana state certificate number received from the state when registering for a state sales tax license (9 or 10 digits)."""

    license_type: LicenseType
    r"""Louisiana license type, which determines the filing portal: Direct Marketers file using the Louisiana DOR LaTAP website, Remote Sellers file using the Remote Sellers website, and Standard license types file using the Parish E-File website."""

    naics_code: str
    r"""6-digit NAICS code used to categorize business operations. Many codes were updated or created in 2022."""

    registered_email_address: str
    r"""Email address registered with the Louisiana Department of Revenue."""

    mfa_completed: Optional[bool] = False
    r"""Whether the customer has completed MFA setup using the Kintsugi virtual mail group ID on their Louisiana tax account."""

    last_payment_to_state: OptionalNullable[str] = UNSET
    r"""Amount of the most recent payment made to Louisiana, as a plain decimal number. Required for Direct Marketer license types; enter 0 if no return has been filed yet."""

    zip_code: OptionalNullable[str] = UNSET
    r"""ZIP or Canadian postal code on the Direct Marketer license registration. Required for Direct Marketer license types. Accepts a 5-digit US ZIP or a Canadian postal code (e.g. L6H0C3)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["mfa_completed", "last_payment_to_state", "zip_code"])
        nullable_fields = set(["last_payment_to_state", "zip_code"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
