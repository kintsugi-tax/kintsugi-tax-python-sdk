"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .connecticutregistrationtype import ConnecticutRegistrationType
from kintsugi_tax_platform_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class ConnecticutFieldsTypedDict(TypedDict):
    r"""State-specific fields for Connecticut registration import (myconneCT / DRS)."""

    registration_type: ConnecticutRegistrationType
    business_name: str
    r"""Business name as registered with the state of Connecticut."""
    ct_tax_registration_number: str
    r"""Connecticut tax registration number issued by DRS."""
    last_payment_to_state: str
    r"""Last payment made to the state as a plain decimal (e.g. 0.00 if no payment has been made; do not include $ or commas)."""
    registration_id: NotRequired[Nullable[str]]
    r"""Connecticut DRS registration number used on the filing (AP) report; distinct from the myconneCT login credentials."""
    mfa_completed: NotRequired[bool]
    r"""Whether the customer has completed MFA setup in their Connecticut tax account."""


class ConnecticutFields(BaseModel):
    r"""State-specific fields for Connecticut registration import (myconneCT / DRS)."""

    registration_type: ConnecticutRegistrationType

    business_name: str
    r"""Business name as registered with the state of Connecticut."""

    ct_tax_registration_number: str
    r"""Connecticut tax registration number issued by DRS."""

    last_payment_to_state: str
    r"""Last payment made to the state as a plain decimal (e.g. 0.00 if no payment has been made; do not include $ or commas)."""

    registration_id: OptionalNullable[str] = UNSET
    r"""Connecticut DRS registration number used on the filing (AP) report; distinct from the myconneCT login credentials."""

    mfa_completed: Optional[bool] = False
    r"""Whether the customer has completed MFA setup in their Connecticut tax account."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["registration_id", "mfa_completed"])
        nullable_fields = set(["registration_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
